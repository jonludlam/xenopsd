(jbuild_version 1)

(executable (
  (name dbgring)
  (libraries (
    xapi-xenopsd
    xenctrl
    xenstore
    xenstore.unix
    xenstore_transport
    threads
    stdext
    xcp.xen
    rpclib
    uutf
    xcp
    rpclib.json
  ))
))
