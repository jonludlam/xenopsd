(jbuild_version 1)

(executable (
  (name dbgring)
  (public_name dbgring)
  (package xapi-xenopsd-xc)
  (libraries (
    xapi-xenopsd
    xenctrl
    xenstore
    xenstore.unix
    xenstore_transport
    threads
    stdext
    xcp.xen
    rpclib.core
    uutf
    xcp
    rpclib.json
  ))
))
